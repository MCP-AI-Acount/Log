<!-- MIRROR-GENERATED -->
> [!warning] 생성 파일 — 여기서 편집하지 마세요
> 이 문서는 `automation/obsidian_mirror/Harness/A_Series_Spec.md` 에서 자동 생성된 **사본**입니다.
> 여기 가한 수정은 다음 동기화(`obsidian_archive.regenerate_rules_mirror`)에
> 정본 내용으로 **덮어써져 사라집니다**. 고칠 곳은 위 정본 경로입니다.

# A 시리즈 스펙 (품질 공리 · v2)

> 2026-06-20 세션 합의 · 2026-06-21 A 시리즈 정본화 완료.
> **정본 정의·가중치·관계망** → `cursorrules_Essence.mdc` §0-A2 · §1 · §1-B.
> 본 문서 = 결정 기록(decision record).

## A0 · 지향 전문 (telos — 채점 안 함)

- **자동화 · 체계성 · 적용성** — 끝점 없는 방향(벡터). 헌법의 "전문"에 해당. 품질 원자 아님.

## 품질 원자 — 프로세스 3 × 층 3

| 프로세스 | 바닥 (관통·소극·−) | 이해 (파악) | 달성 (목표·적극·+) |
|---|---|---|---|
| 의도 | 보존성 (A1) | 분별성 (A4) | 확실성 (A7) |
| 실행 | 직접성 (A2) | 명료성 (A5) | 정합성 (A8) |
| 확인 | 일관성 (A3) | 검토성 (A6) | 완결성 (A11) |

- **소극(바닥)** = 모든 단계 관통하는 제약. **적극(달성)** = 그 단계에서 이루는 목표.
- **절약성(Ac)** = 비용 분모(토큰·시간·코드 간결성). 전 칸을 균일 관통.

## 방향성 (벡터)

- **범용성 (A9, 내부)** ↔ **확장성 (A10, 외부)**. 일반화를 어디로 겨누나.
- 마더십 전환으로 외부극 약화 → 확장성 = `projects/<id>` 하위프로젝트 이식, 범용성 = 코어 일반화.

## U 옆판 (유저 경계 — A 아님)

- **제어성** → U 시리즈 (§0-A-U). "품질"이 아니라 "유저·수행 관계".
- **완결성(A11)** = 확인-달성 축 — U 이관 없음.

## PR 연결

- "분해(요청을 쪼개 읽는 행위)"는 **PR1 읽기규율** 문구 수정으로 흡수.
- "분해가 잘 된 정도(품질)"는 **분별성(A4)** — 채점. 둘은 교차참조, **이중채점 금지**.

## 가중치 (Essence §1 · Value §4 요약)

| 코드 | 가중치 |
|---|---|
| A3 | ×2.0 |
| A8 | ×1.7 |
| R1 | ×1.7 (조건부) |
| A5 | ×1.6 |
| A9 | ×1.6 |
| A4 | ×1.5 |
| A1 | ×1.3 |
| A7 | ×1.2 |
| A11 | ×1.1 |
| A10 | ×1.0 |
| Ac | ×1.0 |
| A6 | ×0.9 |
| A2 | ×0.7 |

> D-10 가중치 — sudoku 방식(행·열 합 4.0 마방진) + per-cell override. 절약(Ac)·방향(A9/A10)은 격자 밖.

## 마이그레이션 상태

1. ✅ `cursorrules_Essence.mdc` — A 정의·가중치·§0-A-auto
2. ✅ `cursorrules_Value.mdc` §4 — A 가중치·Δ 테이블
3. ✅ Core · agent-tooling · Detail · registry · automation — A 코드
4. ⏳ 대시보드(`dashboard_value_panel.py`·`app.js`) — 별도 동기화 턴
