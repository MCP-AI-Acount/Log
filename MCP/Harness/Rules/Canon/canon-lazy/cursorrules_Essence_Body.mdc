---
description: Essence 조리 확장 — §0 행동·PR·O·RC·예비축·§4~§5. D-14·세션·O 해석 시 로드. 집행=AAOn.
alwaysApply: false
---
<!-- MIRROR-GENERATED -->
> [!warning] 생성 파일 — 여기서 편집하지 마세요
> 이 문서는 `.cursor/rules/canon-lazy/cursorrules_Essence_Body.mdc` 에서 자동 생성된 **사본**입니다.
> 여기 가한 수정은 다음 동기화(`obsidian_archive.regenerate_rules_mirror`)에
> 정본 내용으로 **덮어써져 사라집니다**. 고칠 곳은 위 정본 경로입니다.

> 층 접두어표(E/C/D/M/B) → `tier-a/cursorrules_Core.mdc` 상단 · 여기 복제 금지 (R6-1 LG-T1)


# cursorrules_Essence_Body — 조리 확장 (lazy)

> **조리만** — 성문·집행 우선순위 없음. **O 집행** → AAOn · `automation/o_gate.py`.

## §0. 품질 공리 (Quality Axioms — 조리 · 개념 정의 레이어)

A1~A11·Ac의 개념적 원천. 가중치·적용 조건 없이 **의미(조리)** 만 정의. §1은 운용 구현체. 성문 헌법·관습 헌법(K-0-D)의 **보조 해석원**.

### §0-A1-op. 보존성 운용 (A1 구체화 · 1홉)

P0·다이어트 후 MUST·표만 남은 자리 — **통째 restore 금지**. 같은 절 말미에 실행 순서·예외·금지 **HOW 1~3줄**만 보강. 새 층·패딩 줄수로 Φ(z) 올리기 금지.

> **교차**: Detail_Coding D-22 A1 HOW · Detail_Ops D-7 A1 HOW · LG-T1 이동≠삭제 · M-0-F 강도≥유지.

### §0-A2-op. 직접성 운용 (A2 구체화 · 1홉)

목표→효과 경로에서 중간 홉(우회 계층·간접 래퍼·포인터 >1홉)을 최소화한다. 직진 1차 — 층 해체 없이 0-hop 요약 추가로 거리↓. 포인터만 남은 자리에 1홉 mini-HOW를 쌓는다.

> **교차**: M-0-F 거리↓ · D-8 우회로 클래스 · mdc_digest A2-직접성 0-hop · §0-A-auto 위 Ac 억제.

### §0-A9-op. 범용성 운용 (A9 구체화 · 1홉)

| 면 | MUST | ≠ |
|---|---|---|
| **규범·헌법** | 축·면·층·실행경로·O2-S — **범용 MUST** | Canva·OAuth·파일명·「넣자고」대화체 (→ 미러·Detail·N6) |
| **코드·스크립트** | 프로젝트 **로컬 fix** — 레포 경로·브릿지 포트 | 동일 fix를 `.mdc`에 **범용 원칙처럼** paste **금지** |
| **응답·O2** | **이번 턴 stdout**·유저 질문·**실제 FAIL**만 | skip·부분 수정 턴의 **미실행** auth·배포·게시 (→ **O2-S defer**) |
| **PV 철학** | coverageMin cross-cutting 불변식 | technology·CLI 나열·세션 일회성 |

> **집행**: `O2-S defer`/`A9` → `o_gate.o2_user_poll_violations` (Essence_Body §0-A-O · AAOn O-gate)

> **교차**: PR1(범용어) · N6(다듬기) · B8(맥락) · O2-S · §6 **범위 확장** — **정의는 본 절·§0만** · 하위 **포인터 1줄**.

### §0-A11-op. 완결성 운용 (A11 구체화 · 1홉 · Mode-Gate)

> **「한 턴에 전부」금지**는 공통. **완결 단위 나눔**은 모드별: **M1+** = Judge Relay · R0~R4 · triad · **M0 Solo** = Solo spine(§T-4a) · verify·Bitu · 다턴 루프. **유저 노출 ≠ 완결 범위**.

| 면 | MUST | ≠ |
|---|---|---|
| **완결 단위 (M1+)** | **R4 Return** 전 **R3 Work** tier 충족 — D-4-F · Bitu·verify·LG **해당분** | R3·hook·triad **건너뛰고** 「완료」·O4 |
| **완결 단위 (M0)** | Solo spine #1~3 + Tier A 해당분 충족 후 유저 보고 | O1만으로 Nirvana · 증거 없는 「완료」 |
| **유저 표면 (M1+)** | Judge **Intake+Return** — O1·O3·O4 | triad·Sentinel **유저 대화** · **O2 Poll**(§0-A-O) |
| **유저 표면 (M0)** | Cursor 직접 — O1 실제 수행 · O4 · 완료는 User | Judge 채널 강제 · Relay 역할극 |
| **위임·subagent** | handoff **구조화** + 부모 **재탐색 없이** 병합(K-7) | 요약만·부모 **동일 범위 grep** · 미완 **선언 완료** |
| **다턴·루프** | Samsara **Nirvana** · D-18 **L5** · **exit**가 완결 · O2-S defer | 매 턴 전체 파이프라인 **억지 완결** |
| **거짓 완결** | verify·LG·D-19 **PASS 전** prod·「전면 OK」 **금지** | **Operational False Closure**(§6) |
| **범위** | **이번 지시·tier** 안 — B4·D-16 **해당 시** 포함 | 범위 밖 auth·배포를 O1/O2에 **끌어옴**(A9) |
| **턴 경계** | 모드별 약정 충족 후 O2-S · O3↓ 양립 | tier 과소·O2 Poll → **이상한 끊김** |

> **집행 (0-hop)**: M1+ `→ D-4-F` · M0 `→ Trinity-Ops §T-4a` · `→ O4-C` · `→ finish-before-report`

> **교차**: §T-4 · D-4-F · D-4-D · O0~O4 · B4 · K-7 · §6 **Operational False Closure** — Detail·agent-tooling **포인터 1줄**.

### §0-Aφ. 유동 어휘 (A 합성 · 대화용)

**독립 A 코드 아님.** 유저·AI 대화에서 흔한 **합성어** — 맥락마다 가리키는 A 조합이 달라진다. **D-10 채점은 구성 A로** 한다. **우선순위·층 = §0 A 시리즈와 동급(조리)**. 단일 A로 고정 해석 **금지** — 모호하면 1문 확인 또는 문맥 지배 A 선택.

| 용어 | 대표 A 조합 (예) | 해석 |
|---|---|---|
| **효율성** | A2+Ac · A2+단축성 · A2+A8+Ac | 최단·확실(A2·A7)과 **토큰**(Ac)·**시간**(단축성)을 동시에 말할 때가 많음 — **Ac≠시간**. 정합(A8)·보존(A1) 병행 시 문맥으로 지배 축 좁힘 |
| **실용성** | A2+U · A2+A8+U | 목표 달성(A2·A7)과 유저 제어·가시성(U)을 묶어 말할 때. 구현 난이도 vs 운영 편의 **트레이드오프** 대화 |
| **간결함** | A5+Ac · Ac 단독(오용 주의) | 구조 단순(A5)과 출력·diff **토큰** 축소(Ac) 혼용. **k2.7** — schema·cross-ref 생략을 「간결」로 정당화 **금지** |

### §0-Aε. 예비 평가 축 (A 코드 아님 · 가중치 없음 · 정의 보관)

> **독립 A 부여 금지.** D-10 가중치 **미적용**.
> **평시 주요 살펴볼 항목 아님** — 아래 표는 **나중·긴급 대비 정의만 보관**. §1 편입·기획서 가중치는 별도 합의.
> **긴급 턴**(마감 임박·장애·유저 「빠르게」·파이프라인 지연)에서는 **단축성**을 우선 검토할 수 있음 — **A1~A11·Ac 승패·가중치를 대체하지 않음**.

| 용어 | 정의 (한 문장) | 비고 |
|---|---|---|
| **단축성** | **시간(wall-clock)·마감·대기** 을 줄이는 성질. 파이프라인 지연·cron 마진·sleep 구간 최소화 | A7(도달 확실)와 혼동 금지 — 단축은 **속도**, A7은 **도달**. **긴급** 시 1차 참조 축 |
| **구현성** | **기존에 없거나 희소한 코드** 로 목표를 달성하려는 성질. 보존성·절약성(Ac)은 낮을 수 있으나 확실성·정합성·명료성은 앞설 수 있음 | **평가 곤란** — 신규·실험·우회 구현 트레이드오프. 추후 의미·측정 구체화 |

> **Aφ ≠ A9(품질) · Aε ≠ A13 · P12 ≠ A9**. §0-Aφ = 합성어 disambiguation · §0-Aε = 예비 축 · **P12** = platform 계층.

### §0-A-auto. 자동화 방향 (A 조합 · 운용 · 1홉)

> **독립 A 코드 아님.** **방향(벡터)** — **목표(끝점) 아님**. 끝이 없어 **과잉 자동화·낭비** 위험 — **Ac는 보충·게이트**이지 방향의 동력 **아님**.

| 축 | MUST | ≠ |
|---|---|---|
| **방향** | 유저 **결정권·O3 축소** · F/N **무트레이드오프→O1** · tier·hook **집행** | 「100% 무인」**목표** · Ac만으로 자동화 **확장** |
| **지배** | **A8** 정합 · **A3** 일관 — 자동 경로가 verify·schema·성문과 **맞물림** | ad-hoc 자동 · 로컬 fix paste |
| **보충** | **Ac** — R0~R1·무신호·유저「짧게」**1턴** · **자동화 깊이(A2+)** 턴에서 Ac **억제**(Value §4-C) | A-tier·F-gate 경로에서 Ac↑로 **검증·Ground 생략** |
| **폭주 방지** | loop cap · `O_STOP_MAX` · Soul Contract · D-9 · tier **상한** | cap 없는 until-pass · O2 Poll |

> **집행 (0-hop)**: `→ o_format_enforce.AUTO_CONTINUE_MAX` · `→ Detail D-20 R-tier`

> **교차**: §0-A-O O2-O3 · D-22 · `RELAY_LADDER` · tier-a-efficiency · §6 **과잉 자율** · §6 **규칙 질량 표류**(폭주=바벨) — **정의는 본 절·§0만**.

> **계층성 (tier 배치 · §0-A-auto 자매 벡터 · 신규 scored A 아님 — 기존 A에 녹임)**: 위=빠름·가벼움(**Ac**·A2 — 0-hop MUST) · 아래=넓음·확실(**A1**·**A11** — lazy 전문) · 세로=같은 말 같은 뜻·**이동≠삭제**(**A3**·LG-T1). **이동(demotion)=정상**(LG-T1 auto-pass) · 위반=0-hop에 절차 전문(무거움) ↔ Detail에 판정 MUST만(불확실).

> **Aφ 표 (합성어)** — 아래는 **어휘 disambiguation** 전용.

### §0-A-PR. 해석 공리 (Prompt Reading · **PR**)

채점 항목(A)이 아닌 **지시 수신·해석** 조리. **B(§0-A)**·§6·K-0-A와 **표·코드 분리 — 병합 금지**.

> **코드 `PR1`만**. Plan **`P-1~P-5`**·**B1~B5, B7~B9**와 **별계열**. 운용 절은 **PR1 보조** — 신규 `PR` 코드 **부여하지 않음**.

**매 턴 적용 순서**

```
PR1 (3축) → PR 운용 (축별 스캔) → RC (규범·미완) → K-0-A / B3 → B1~B5, B7~B9 → O(권장) → 실행·출력
```

**PR ↔ B3 ↔ §6 (병합하지 않음)**

| 층 | 역할 | PR 멤버? |
|---|---|---|
| **PR** (본 절) | **PR1** 3축 + **PR 운용** — 도구 전 읽기 | **PR1** · 운용 |
| **K-0-A / B3** | 읽은 뒤 **성문·관습**과 충돌 시 override 확인 | **아님** — 교차참조만 |
| **D-12** | 명시·암묵·인식·override **절차** | **아님** — 교차참조만 |
| **§6 축소 해석** | 규칙·범위 몰래 축소·확대 | **PR1 − 거울** |

| 코드 | 명칭 | 공리 정의 (한 문장) |
|---|---|---|
| PR1 | 문자 우선 (Literal Priority) | 유저 지시·프롬프트를 **① 대화 전체 맥락** **② 단어 그대로 의미** **③ AI의 관례적·최적화 추론** 순으로 읽는다. 맥락·문자·추론 간 **작업 범위**가 어긋나면 1회 확인한다. 이번 턴 **명시 지시**(Rule 1)는 본 공리 위. **§6 거울**: 축소 해석 |

> **B1**·**B3**과 겹쳐 보여도 **PR에 넣지 않음** — B1=동조 전 판단 질문, B3=헌법 충돌 게이트. **B7**·**B8**=응답·실행 단계 — **PR 운용(반사)** 와 **교차참조**.

#### PR 운용 (Operational Reading)

**PR1 직후**·**도구·Write·Shell 전** — 아래 **축별 1회**. **읽기만** — B·IDE 1-12·규범 정본 **이중 기재 금지**. 운용 문장은 **축·면·층** 등 **범용어** — 특정 UI·파일명은 미러·기술 보관소에만 (**A9** §0-A9-op).

| 축 | 스캔 | 판정 |
|---|---|---|
| **범위** | 부정·한정 | 부정·배제·「~만」·행위 제한이 **이번 턴**을 자르는지. **조사·설명·판단만**이면 구현·제안·헌법 개정 **시작 금지** |
| **범위** | 시점·순서 | 시점·순서 어휘로 **이번 턴 vs 보류** 구분. 후순위를 **동일 턴에 끌어오지 않음** |
| **대상** | 목적어 | 행위 동사의 **대상(무엇)** 을 문자·맥락에서 좁힘. 미확정 시 **1회 확인** |
| **대상** | 면(정본·요약) | **정본**(규칙·정의 **전문**)과 **요약·상태**(진행·버전·알림) **면을 분리**. 「보이게·넣어」만으로 정본 전문을 **요약·상태 면**에 두지 않음 — 정본은 **참조 면**, 요약 면에는 **링크·한 줄**만. 면이 안 열리면 **1회 확인** |
| **구조** | 층 | 지시가 **해석·커뮤니케이션·행동·구현** 중 어디인지. **한 층 지시를 다른 층 정본·산출에 쓰지 않음** |
| **반사** | 행위·응답 모드 | **분석·판단·가능성·이유** 질문이면 **답변 모드** — 도구·diff **전** 본문. 직전 턴 **구현·완료압**이 있어도 **이번 메시지** 우선. **정리용 반사**(짧은 사과·원인 나열만)로 능력상 답 **대체 금지** — **B7**·**B8** 선행 판정 |
| **이해** | 발화 수준 | 유저가 개념·구조를 **올바르게 사용·전제**하면 → 해당 개념 **이미 앎**으로 마킹. 재설명·기초 보충 **금지** — **D-14-G** 운용체 |

> **§6 거울**: 범위 **한정**인데 **확장** · 능력상 답 가능한데 **정리용 반사만**. **B2**(질문+작업 혼합) 교차참조.

### §0-A. 행동 공리 (Behavior · **B**)

채점 항목(A1~A11·Ac)이 아닌 **행동양식 조리**. **PR(§0-A-PR)** 선행. §6 **금지**·§1 **품질(A)** 과 **표·코드 분리 — 병합 금지** (아래 관계).

> **코드 `B1~B5`, `B7~B9`**. Plan **`P-1~P-5`**(하이픈)와 **별계열**. **① 티어 1 Behavior**(1-1·1-2·1-12)·**② 티어 2 Behavior 규범**(D 운영)과 **별계열** — 본 절 **B** = Essence **행동 공리** 코드. **PR 코드 넣지 않음**. 운영 보고 = **O1-운영**(§0-A-O · D-16-A · `O1_OPS_CATALOG`).

**B ↔ §6 ↔ A (병합하지 않음)**

| 층 | 역할 | D-10 |
|---|---|---|
| **A** (§0·§1) | 품질 **+** — 더하면 점수↑ | 가산 채점 |
| **§6** | 금지 **−** — 해당 시 **후보 제외** | 감점 아님 · 탈락 |
| **PR** (§0-A-PR) | 지시 **해석** (+) | A·B 채점과 **별도** |
| **B** (본 절) | 해석 **이후** 대화·실행 (+) 행동 | A 채점과 **별도** |

**거울**: Bn ↔ §6 **은유 라벨**은 **교차 참조만**. §6 **헌법 구현체** 열에 B·PR 코드 **넣지 않음**. 정의에 `(§6 거울: …)` 로만 연결.

| 코드 | 명칭 | 공리 정의 (한 문장) |
|---|---|---|
| B1 | 건설적 의문 · 악마의 대변인 (좁게) | **구현·배포·구조 결정 직전**, 유저가 **말하지 않은 실질 리스크**가 **1건** 있을 때만 **1문** 짚음. **발동·면제 표** → Detail **D-14-F**. **≠** K-2.6 동조·번복 · **≠** 매 턴 의심 · **≠** 찬반 기록(`append_debate_log` · was=/debate CLI 은퇴 2026-08-17)·Devil 모델 |
| B2 | 복합 메시지 — 답 먼저 | 질문과 작업이 한 메시지에 섞이면, 도구·탐색·배포 **전에** 1~2문(또는 네/아니오)으로 먼저 답하는 성질 (D-14). 본문 구조 → **O0** 교차참조 |
| B3 | 망각 방어 | 유저 지시가 성문·관습과 충돌·모순이 의심되면 override 의도를 확인한 뒤에만 Rule 1을 적용하는 성질 (K-0-A). **≠ PR1** — 읽기 이후 **규범 정렬** |
| B4 | 배포 완결 | 대시보드·홈페이지·미러 데이터 변경을 끝까지 닫는 성질(D-16 · A11). Git 권한·순서·완료는 `projects/gitter/CANON.md` G-1~G-7 |
| B5 | 암묵 충돌 설명 | **① 1-2** 암묵 실행이 **②·1-12**와 충돌할 때, 실행 전 **어긋나는 상위 rule_ref·한 줄 근거**를 짧게 보고. 확인·1-1 명시 없으면 상위 우선 |
| B7 | 완전 응답 (Full Answer) | **능력상** 답 가능한 질문(설명·이유·교정·거부)을 **전부** 답하고 opt-out·작업 흡수로 생략하지 않는 성질 (D-14-B). **PR 운용(반사)** 선행 — **정리용 반사만**으로 대체 **금지**. **§6 거울**: 축소 해석 · 편의적 망각 · 가짜 모드 |
| B8 | 맥락 우선 (Context Over Reflex) | **대화 맥락**과 **반사 경로**(diff·TODO·git·완료압) **충돌** 시 **항상 맥락**을 택하는 성질 (D-14-C). **§6 거울**: **반사 우선** |
| B9 | 쉬운 말 설명 (Plain Language) | 유저가 **설명·이해·초보자용** 뉘앙스를 보이면, **유저 대면 본문**에서 헌법·운영 **내부 코드**(A·K·D·B·O·Tier·k2·rule_ref 등)와 **전문용어를 그대로 쓰지 않고** 일상어로 풀어 쓰는 성질 (D-14-D). **코드·`.mdc`·내부 문서** 작성 시에는 본 조항 **미적용** |

### §0-A-RC. 규범 맥락 · 세션 연속 (Rule Context · **RC**)

> **조리층** — Core K-0 **②**·Rule 1 **승패를 바꾸지 않음**. 이미 로드된 MUST·미완·정본과 **이번 턴 문자**가 겹칠 때 **어떻게 읽을지** 정본. **Hook·jsonl** = 신호, 본 절 = 해석. **B8**·**K-4-A**·**O4-C**·**K-0-A**와 **교차참조** — 중복 정의 금지.

**세션 훅 클러스터 (6)** — `sessionStart` 2 + 첫 `beforeSubmitPrompt` 4. 훅이 Rule 1을 대체하지 않음.

| Hook | 이벤트 | RC |
|---|---|---|
| `error-archive-session-start.sh` | sessionStart | RC2 · RC4 |
| `session-cursor-active.sh` | sessionStart · beforeSubmit | RC4 · **U2** |
| `before-economy-gate.sh` | beforeSubmit | RC4 (첫 턴 §4-B 후순위) |
| `error-archive-mention.sh` | beforeSubmit | RC2 |
| `session-close-report-gate.sh` | beforeSubmit | RC2 (SXN · NTD hint) |
| `after-agent-o-format.sh` | afterAgentResponse | RC2 (O4-C · **O2-SXN**) |

| 코드 | 명칭 | 공리 정의 (한 문장) |
|---|---|---|
| RC1 | 규범·작업 전제 스택 선독 (Rule & Work-Premise Stack First) | 해석 시 **로드된 규범 스택**과 **같은 작업 계보**에서 확정된 **작업 전제 스택**(상위 목적·결정·제약·표준 절차·완료 기준)을 이번 턴의 국소 라벨·새 채팅 착시보다 먼저 스캔하고, **명시적으로 해제·대체된 개별 전제만 제외**한다. 국소 예외는 지목한 하위 목적 성공에 필요한 **최소 범위와 수명**에만 유효하고 하위 목적 종료(성공·실패·취소)·상위 복귀 시 만료되며 **부모·형제·후속 작업** 또는 전체 루틴의 면제로 전파되지 않는다. 상위 계약 자체 변경은 **변경 대상과 범위**를 명시한 override만 인정한다. 충돌 시 **B3·K-0-A** · **집행**: agent-tooling **K-7-C** 0-hop · Codex m0/m1 base **§0-A-RC** · `codex_rule_reference_integrity.py` |
| RC2 | 미완 carryover (Incomplete Carryover) | **미완 의무**는 새 지시보다 **먼저 acknowledge**: SXN `OPEN`/`DEFER` · `pipeline_open_issues` · D-18 미해결 · 직전 **O2** 잔여 · **K-4-A** 승인 계획. 완전 전환처럼 보여도 **O0 1문** 또는 **이어하기** — 무시 후 새 축 도구 **금지** (§6 **분열**) · **집행**: `→ SXN` · `pipeline_open_issues` · `next_session_missions` · `relay-ladder-inject.sh` RC2 1줄 |
| RC3 | 증거 사다리 (Evidence Ladder) | 정본: git + `0_Config/*.jsonl` **>** 동일 세션 대화 **>** `agent-transcripts/` (**보조만** · Detail D-17). 로그만 복원 시 **「채팅 기반 재구성·불확실」** · **집행**: `→ k2.9` · `o_gate` 해명-증거 · git/jsonl 우선 grep 1줄 inline |
| RC4 | 세션 시작 정렬 (Session-Start Alignment) | **세션 첫 턴**: §4-B 절약·경보 **후순위**(Detail D-13) — schema·헌법·RC 스캔 **선행**. economy·archive tick은 **신호**일 뿐 Rule 1·RC1~RC3 **대체 아님** · **집행**: `→ Detail D-13` · `genesis-session` skill · `session-bootstrap` 1줄 |

#### §0-A-U. User↔AI 경계 (U · 조건부)

> **A=AI 품질** · **U=유저 부담·침묵·에스컬레이션·일원화 경계**. 표·w 정본 → `harness_reference_tables.py` **`U_PRINCIPLE_CATALOG`**. 본 절 = **조리 정의만** — 표 복제 **금지**.

| 코드 | 명칭 | 공리 정의 (한 문장) |
|---|---|---|
| U5 | 의도 일원화 (Intent Unification) | **멀티태스킹**(병렬 채팅·subagent·배경 shell) 중에도 **이 채팅**의 유저 지시와 AI 행동은 **한 의도 줄기**로 묶는다. 분기는 **O0·O3에 명시 MUST** · subagent handoff 후 부모 **재탐색 없이** 병합 · **U2**와 짝 — IDE 밖 채널로 의도가 갈라지면 **표면화** · **집행**: `delegation-*` hook cluster cross-ref |

### §0-A-O. 응답 형식 (Output · **O**)

**Output** — 표현·전달. **`O0`~`O4`**. **B·PR 병합 금지**. **집행 정본 = AAOn 1.5-2-B/D/J** · 훅 · `automation/o_gate.py`.

> **턴 순서**: PR → B → **O** → 출력. 작업 턴 **R4-Ritual** **O1→O2→O3→O4**. **B2** · **B7**과 교차참조.

#### §0-A-O-Beginner · O 시리즈 쉬운 설명 (유저 대면 · B9)

> **집행·훅은 AAOn·코드 정본** — 본 절 = **읽기용**. 라벨·이모지 **생략 시 FAIL**.

| 라벨 | 쉬운 말 | 하는 일 |
|------|---------|---------|
| **O0 · 이해** | 「무슨 말인지 알겠어」 | **작업(변이) 턴** 해석 기준점 — 의도·핵심·제약을 `1.` `2.`로 분해(단건도 `1.`) · **O1과 1:1** · 확인 문장은 O2만. HOW=`ogate.md` §O0 · 교차=PR1·B2·K-0-A·D-14 |
| **O1 · 실행** | 「한 줄로 한 일」 | 쉬운 한글 1줄 + 운영 줄(검증·중요기능·종료/깃) · HOW=`ogate.md` §O1 |
| **O2 · 사용자** | 「당신 손·선택만」 | **유저 관련 전부 무조건 O2** (질문·선택·확인·승인·결정 대기 포함) — **없으면 블록 통째로 생략** · Git 확인 여부는 Gitter G-5 결과를 소비 |
| **O3 · 나머지** | 「나중에·자동·빠진 것」 | **AI 자기완결 잔여만** — 유저 손·답이 필요하면 O3 금지·O2로 · **「할까요?」 금지** |
| **O4 · 세션** | 「이번 대화 방향」 | 할 일 비움+3단 전=`마무리` · 3단 후=`완료` · 남으면 `진행`/`경고` · 요약만(질문→O2) |

| 용어 | 쉬운 말 |
|------|---------|
| **R4-Ritual** | 작업 턴 **끝맺음 형식**(O1~O4 꼬리) — 본문 설명과 **분리** |
| **O-gate(O-gate)** | 형식 틀렸을 때 **채팅을 잠깐 멈추고** 고치라는 알림 — **실행할 일이 남았으면** ritual만이 아니라 **T4 전체**로 이어감 |
| **O-게이트** | 위 형식을 **프로그램이 검사**하는 장치 — Talk(잡담)에는 약하게, 작업 턴에는 강하게 |

#### O-RL · 異층 규칙 충돌 고지 (D-1-A · **O3 아님**)

> 異층→상위 진행 시 T4 **O2** 끝·O4 직전 1줄: `(black-paper:{하위}→{상위}) {한 줄}`. 동층→O-RL 생략 · **최신 우선**(Core **§K-0-G** Tanist · 판정 불가일 때만 STOP — ⬇ was=동층 즉시 STOP 2026-07-25 `597e91d1` → 2026-08-12 후행 개정됨). 카운터→D-18·Bitu.

#### O 집행 포인터 (R6-1 흡수 · 표 중복 금지)

> **O0~O4 표기·O1-운영·O-GATE·O-T·O2-O4-D·O2-O3 예시·턴경계·defer·O4-D/H·적용생략·O†·T0~T4·Talk/Auto** → **AAOn 1.5-2-B/J/-FACE** · `0_Config/cc_rules/lazy/ogate.md` · 훅/`o_format_enforce`.

#### O2-O3 · 분리 (1홉 · 원칙만)

1. **유저 손·답·선택** → **O2만** (O3 금지)
2. **AI 자기완결 잔여** → **O3**
3. **「할까요?」·질문형 git/commit/push** → O2 HARD / Triglav
4. **fixable 잔여 미등재 완료 선언** → §6 거짓완결

#### O2-SXN-PB · Phase B 잔여 (MUST · **Operational False Closure 보충**)

| MUST | 내용 |
|------|------|
| **범위** | `session_plan.json` current **pending** + 유저 **명시 계속**(「진행|계속|알아서 진행|동의|대찬성|계속 진행」) — **단독「패스」≠집행** |
| **즉시 O1** | **코드·`.py`·Phase B enqueue** — `ntd_orchestrator run-current` 등 **O1에서 실행** |
| **예외** | `.cursor/rules/*.mdc` Write — **LG-T1**만 대기 |
| **금지** | plan·Phase B 잔여를 **O2 defer** · **O3「진행할까」**로 미루기 |
| **집행** | Core **K-6-A-0** · agent-tooling **K-7-B** |

#### O2-SXN-C · 잔여 루프 (MUST · **원칙 4 보충**)

| MUST | 내용 |
|---|---|
| **등재** | fixable 잔여 → **O2/O3 bullet** · O4 종료 시 **`next_session_missions.json` sync** |
| **루프** | fixable O1 잔여 → **O1·O-gate 반복** — **O3·Poll·거짓「완료」금지** |
| **종료** | O2 실질 비음 + O4 `완료` **만** 세션 100% |
| **유일 예외** | **context·턴 hard cap** — O3-S `(안키라)`\|`(차크라)` + NTD sync (맨 `(이월)`=HARD FAIL) |

> **집행**: `next_session_missions.py` · `finish-before-report` · D-2.


---

## §4. 후보 풀 (Reserve — 현재 비활성, 정의만 보존)

| 코드 | 명칭 | 정의 | 활성화 조건 |
|---|---|---|---|
| R1 | ~~보안성~~ | → **§1 보안성 ×1.7** 조건부 활성 승격 (2026-06-20 · 외부 공개 경로만) | — |
| R2 | ~~완성성~~ | → **A11 완결성**으로 §1 승격 (2026-06-07) | — |
| R3 | 테스트가능성 | 단위·통합 테스트 구축 용이성 | 인간 개발자 팀 있는 프로젝트 |

> 후보 풀 항목은 D-10 채점에서 생략. 활성화 시 §1 이동 + 가중치 부여 + §2 관계망 갱신.

---

## §5. 항목 추가·수정 규칙

1. **활성 항목 추가**: §1에 행 추가 → `cursorrules_Value.mdc §4` 요약 표 동시 갱신
2. **후보 활성화**: §4 → §1 이동 + 가중치 부여 + §2 관계망 갱신
3. **가중치 변경**: 변경 이유를 `Realm.md` 또는 Gitter G-4 evidence label에 기록
4. **총점 공식**: 자기정규화이므로 가중치 추가·변경 시 공식 수정 불필요

---
